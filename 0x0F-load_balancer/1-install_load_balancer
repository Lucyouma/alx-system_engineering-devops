create a script to install and configure HAProxy on lb-01 server
# HAProxy configuration to send traffic to web-01 and web-02 servers
# Requests distribution using a roundrobin algorithm
# Ensure HAProxy is manageable via an init script

# HAproxy installation and configuration on my lb-01 server.
sudo apt-get -y update
apt-get -y install haproxy

# edit config file
server_config=\
"
frontend  lucyouma_frontend
        bind *:80
        mode http
        default_backend besthor_backend
backend lucyouma_backend
        balance roundrobin
        server 464782-web-01 54.160.74.212:80 check
        server 464782-web-02 54.172.239.29:80 check
"
echo "$server_config" | sudo tee -a /etc/haproxy/haproxy.cfg

# HAProxy enabling to be started by init script
echo "ENABLED=1" | sudo tee -a /etc/default/haproxy

# HAproxy configuration file testing
sudo haproxy -c -f /etc/haproxy/haproxy.cfg

# Nginx service restart
sudo service haproxy restart
